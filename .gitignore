.nvimlog
/.claude
# Claude Code runtime state inside the package directory
claude/.claude/**
!claude/.claude/
!claude/.claude/settings.json
!claude/.claude/statusline-command.sh

# Lazygit state file (auto-generated)
lazygit/Library/Application Support/lazygit/state.yml

# Legacy repo-local git identity override (kept ignored for compatibility)
git/.gitconfig.local
